function Loading() {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
        height: "100vh",
      }}
    >
      <h2 style={{ textAlign: "center" }}>Loading...</h2>
    </div>
  );
}

export default Loading;
